export type PaymentResultModel = {
  tax: string;
  total: string;
  subtotal: string;
  taxValue: string;
  taxLimit: string;
  taxTotal: string;
  unitaryPrice: string;
  numberPatients: number;
};
